<% content_for(:title_tag) do %>
  <%= @thesis.title %>
<% end %>

<main class="container full">

  <div class="row">

    <div class="col col-5" style="background-color: #EEE;">
        <%= image_tag "http://placehold.it/350x500", id:"thesis-cover", class:"col col-10 push middle push-center" %>
    </div>

    <div class="col col-7" id="thesis-content">
      <div class="row">

        <!-- bookmark begin -->

        <div class="col col-1">
          <% if current_user %>
            <%= link_to bookmark_user_thesis_path, method: :post, remote: true, id:"bookmark" do %>
              <% if current_user.voted_for?(@thesis) %>
                <i class='material-icons'>turned_in</i>
              <% else %>
                <i class='material-icons'>turned_in_not</i>
              <% end %>
            <% end %>
          <% else %>
            <%= link_to bookmark_user_thesis_path, method: :post, id:"bookmark" do %>
              <i class='material-icons'>turned_in_not</i>
            <% end %>
          <% end %>
        </div>

        <!-- bookmark end -->

        <div class="col col-11 push-left">
          <h2 class="title"><%= @thesis.title %></h2>
          <h4><%= @thesis.user.first_name %> <%= @thesis.user.last_name %></h4>
          <p class="big"><%= @thesis.subtitle if @thesis.subtitle %></p>
          <div class="row">
            <div>
              <p><%= @thesis.school.acronym.blank? ? @thesis.school.name : @thesis.school.acronym %>, <%= @thesis.school.city %></p>
            </div>
            <div class="push-right">
              <p><%= @thesis.year.year %></p>
            </div>
          </div>
          <p><%= @thesis.resume %></p>
        </div>

      </div>
    </div>
  </div>

</main>


